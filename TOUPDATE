# When updating the extension do these steps

## To build the package
* run `vsce package`

## To install the package on local VSCode
* run `code --install-extension language-support-0.0.1.vsix`

## So run the command
* run `vsce package ; code --install-extension language-support-0.0.1.vsix`